# Expo Public Environment Variables
# Copy this file to .env.local and fill in your values
# Variables prefixed with EXPO_PUBLIC_ are embedded in the app bundle
# WARNING: Do not store sensitive secrets in EXPO_PUBLIC_ variables

# Convex Backend
# Run `npx convex dev` to get your deployment URL
# CONVEX_DEPLOYMENT=dev:your-deployment-name
# EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
# EXPO_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# ============================================================
# Convex Dashboard Environment Variables
# These are NOT stored in .env files - set them in Convex:
# Option 1: CLI - npx convex env set VAR_NAME value
# Option 2: Dashboard - https://dashboard.convex.dev
# ============================================================

# Better Auth (required)
# npx convex env set BETTER_AUTH_SECRET $(openssl rand -base64 32)
# npx convex env set SITE_URL http://localhost:8081

# App URL (required for password reset redirects)
# Must match SITE_URL in Convex dashboard
# EXPO_PUBLIC_SITE_URL=http://localhost:8081

# Resend Email (required for password reset, email verification)
# Get your API key at https://resend.com/api-keys
# npx convex env set RESEND_API_KEY re_xxxxxxxxxxxx
# npx convex env set RESEND_FROM_EMAIL noreply@yourdomain.com

# Resend test mode (default: true = only @resend.dev addresses)
# Set to false for production to send to real email addresses
# npx convex env set RESEND_TEST_MODE false

# Optional: Resend webhook verification (for delivery tracking)
# npx convex env set RESEND_WEBHOOK_SECRET whsec_xxxxxxxxxxxx

# ============================================================
# Stripe Payments (Test Mode)
# ============================================================
# IMPORTANT: Use TEST MODE keys during development!
# Test keys start with sk_test_ and pk_test_
# Live keys start with sk_live_ and pk_live_
#
# 1. Enable Test Mode in Stripe Dashboard (toggle in sidebar)
# 2. Get keys from: https://dashboard.stripe.com/test/apikeys
#
# Client (in .env.local) - used for checkout:
# EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxx
#
# Starter tier prices:
# EXPO_PUBLIC_STRIPE_STARTER_MONTHLY_PRICE_ID=price_xxxxxxxxxxxx
# EXPO_PUBLIC_STRIPE_STARTER_ANNUAL_PRICE_ID=price_xxxxxxxxxxxx
#
# Plus tier prices:
# EXPO_PUBLIC_STRIPE_PLUS_MONTHLY_PRICE_ID=price_xxxxxxxxxxxx
# EXPO_PUBLIC_STRIPE_PLUS_ANNUAL_PRICE_ID=price_xxxxxxxxxxxx
#
# Pro tier prices:
# EXPO_PUBLIC_STRIPE_PRO_MONTHLY_PRICE_ID=price_xxxxxxxxxxxx
# EXPO_PUBLIC_STRIPE_PRO_ANNUAL_PRICE_ID=price_xxxxxxxxxxxx
#
# Server (Convex Dashboard) - used for webhook tier lookup:
# npx convex env set STRIPE_SECRET_KEY sk_test_xxxxxxxxxxxx
# npx convex env set STRIPE_WEBHOOK_SECRET whsec_xxxxxxxxxxxx
# npx convex env set STRIPE_STARTER_MONTHLY_PRICE_ID price_xxxxxxxxxxxx
# npx convex env set STRIPE_STARTER_ANNUAL_PRICE_ID price_xxxxxxxxxxxx
# npx convex env set STRIPE_PLUS_MONTHLY_PRICE_ID price_xxxxxxxxxxxx
# npx convex env set STRIPE_PLUS_ANNUAL_PRICE_ID price_xxxxxxxxxxxx
# npx convex env set STRIPE_PRO_MONTHLY_PRICE_ID price_xxxxxxxxxxxx
# npx convex env set STRIPE_PRO_ANNUAL_PRICE_ID price_xxxxxxxxxxxx
#
# Create Test Products (3 tiers):
# 1. Go to https://dashboard.stripe.com/test/products
# 2. Create products with these prices:
#    - Starter: $4.99/mo, $49.99/yr
#    - Plus: $9.99/mo, $99.99/yr
#    - Pro: $19.99/mo, $199.99/yr
# 3. Copy price IDs (price_xxxxx) to env vars above
#    NOTE: Use Price IDs, not Product IDs!
#
# Test Webhook (local dev):
# stripe listen --forward-to https://your-deployment.convex.site/stripe/webhook
#
# Test Cards: https://docs.stripe.com/testing#cards
# Success: 4242 4242 4242 4242
# Decline: 4000 0000 0000 0002

# ============================================================
# Production Email Deliverability (DNS Records)
# ============================================================
# Required DNS records for production email sending.
# Add these in your domain registrar's DNS settings.
# Resend dashboard provides the exact values for your domain.
#
# 1. SPF Record (TXT)
#    Host: @ or yourdomain.com
#    Value: v=spf1 include:_spf.resend.com ~all
#
# 2. DKIM Record (TXT)
#    Host: resend._domainkey
#    Value: (provided by Resend dashboard after domain verification)
#
# 3. DMARC Record (TXT) - Start with monitoring, then enforce
#    Host: _dmarc
#    Value: v=DMARC1; p=none; rua=mailto:dmarc@yourdomain.com
#
#    Enforcement progression:
#    - p=none      (monitoring only, start here)
#    - p=quarantine (spam folder for failures)
#    - p=reject    (block failures entirely)
#
# 4. Resend Webhook (optional, for delivery tracking)
#    URL: https://your-deployment.convex.site/resend-webhook
#    Events: email.delivered, email.bounced, email.complained
#
# Verify setup: https://resend.com/domains
# DMARC reports: https://dmarc.postmarkapp.com (free analyzer)

# ============================================================
# Local Development
# ============================================================

# Example API configuration
# EXPO_PUBLIC_API_URL=https://api.example.com

# Production Bundle Optimizations (SDK 52+)
# EXPO_UNSTABLE_METRO_OPTIMIZE_GRAPH=1
# EXPO_UNSTABLE_TREE_SHAKING=1
