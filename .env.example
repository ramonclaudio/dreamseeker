# Expo Public Environment Variables
# Copy this file to .env.local and fill in your values
# Variables prefixed with EXPO_PUBLIC_ are embedded in the app bundle
# WARNING: Do not store sensitive secrets in EXPO_PUBLIC_ variables

# Convex Backend
# Run `npx convex dev` to get your deployment URL
# CONVEX_DEPLOYMENT=dev:your-deployment-name
# EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
# EXPO_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# Better Auth (set in Convex dashboard, not here)
# Run: npx convex env set BETTER_AUTH_SECRET=$(openssl rand -base64 32)

# Example API configuration
# EXPO_PUBLIC_API_URL=https://api.example.com

# Production Bundle Optimizations (SDK 52+)
# NOTE: Tree shaking is NOT compatible with NativeWind v5 / react-native-css
# Only enable if not using NativeWind:
# EXPO_UNSTABLE_METRO_OPTIMIZE_GRAPH=1
# EXPO_UNSTABLE_TREE_SHAKING=1
